# 智能记账项目约束

## 进入项目

- 先运行 `git -c core.quotepath=false status --short --branch`，确认分支、HEAD、远端和未跟踪文件，再读取 `README.md`、`docs/PRD.md`、`docs/MAINTENANCE.md` 以及与任务有关的真实账本评估。
- 当前 Codex 环境提供 `$jizhang-product-release` 时，用它处理账单语义、真实数据、浏览器验收、Android 打包和主线发布；Skill 不可用时仍以本文件与项目测试为准。
- 以当前代码、测试、版本元数据和实时 API 状态为准。根目录单数 `AGENT.md` 若存在，只视为旧迁移笔记，不得据此覆盖当前事实。
- 只暂存本次明确改动。账本导出、APK、浏览器证据、密钥、环境文件和用户草稿即使位于仓库中，也不得批量加入、改写或清理。已合入 `main` 的旧功能分支只作历史参考，不得因名称较新而当作当前主线。

## 目录与本机文件

- 根目录保留工具默认发现或项目入口所需的文件；可显式寻址的二级配置放在 `config/`。不要为了表面整齐移动 `package*.json`、`index.html`、根 `tsconfig.json`、Vite、Capacitor 或 ESLint 入口。
- 本机私有输入、历史证据和生成制品统一放在被忽略的 `.local/`：MiMo Key 位于 `.local/credentials/mimo-api-key.txt`，升级签名输入位于 `.local/signing/debug.keystore`，APK 位于 `.local/releases/`，历史材料位于 `.local/archive/`。
- 根目录旧 `key.txt`、`debug.keystore`、`.local-archive/` 和 `.toolchain/` 仅作为迁移兼容，不得重新作为文档推荐路径。移动或清理 `.local/` 前必须确认目标、保留签名连续性和真实账本证据。

## 依赖与重复故障

- 在安装依赖、打包或诊断前先运行 `npm run doctor`。不要因 `PATH` 或 `JAVA_HOME` 暂时缺失就判定工具不存在；先读取项目配置并检查项目级工具链，再考虑修改系统环境。
- Android 构建固定使用 JDK 21，SDK 位置以被忽略的 `android/local.properties` 为准，签名输入默认使用 `.local/signing/debug.keystore`。不得把机器绝对路径、密钥内容或口令写入仓库。
- 完整 Android 打包统一运行 `npm run android:build`。该脚本负责设置进程级工具链、从正确目录调用 Gradle wrapper，并核对包名、版本、录音权限、v2 签名和升级证书指纹；不要再复制多层临时命令。
- 调用 `git`、`npm`、Java、Gradle、ADB、SSH 等原生命令后立即读取并判断退出码。命令路径、工作目录或环境失败属于工具链故障，不能报告为代码或构建失败。
- 同一类故障第二次出现时，优先把检查或修复固化到版本化脚本、测试或项目说明中，而不是只在当前终端临时修好；沉淀稳定原则，不记录易变机器状态。

## 数据与账单语义

- 保持本地优先。真实账本、备份和云快照都视为用户数据；迁移或恢复前先保留可验证的原始快照，并验证 LocalStorage 写入、JSON 序列化、读回、校验和与回滚路径。
- 一条父交易表示一次真实独立付款。不同日期、不同订单或多次实付款必须拆为多条；同一次超市结账或同一电商订单的商品放入 `splitItems`，保存为一条可折叠交易。
- 折叠父账单只承担结算和展示。父级 `amount` 是实际支付额，允许与商品合计因优惠、运费或识别遗漏而不同；分类统计必须守恒到父级实付金额。
- 商品分类以 `splitItems` 为真值：全部同类时父级兼容分类跟随明细，混合分类时父级兼容字段为 `其他`，界面显示综合采购。搜索、筛选、编辑和统计都必须读取明细分类，不得把混合父级重复计入 `其他`。
- 不保存支付方式。商户只用于明细搜索和筛选；标签最多保留一个可选场景值，不把商户、分类或支付方式伪装成标签。
- 不因新规则自动批量改写历史账本。只有在证明确有收益、用户明确授权、保留原始数据且迁移可回退时，才更新历史分类或标签。

## 录入、模型与同步

- 文字、图片和语音都先形成可编辑的确认结果，未经用户确认不得写账。批量确认必须原子保存；失败时不得留下半批数据或触发“账本损坏”恢复流程。
- 成功入账后默认留在录入页，清空并聚焦输入框；进入明细必须由用户显式选择。防止双击或重复回调造成重复写入。
- 应用内语音直接采集麦克风音频并使用 MiMo ASR，转写文本必须可编辑后再走统一批量解析；不得把输入法语音当成应用内语音，也不得转写后自动入账。
- 云同步默认关闭并保持本地为主账本。只同步业务账本字段，不上传 API Key、token 或设备凭据；云恢复和整端覆盖必须显式确认，冲突优先按交易 ID 合并并保留本机版本。
- Web 公网界面不是服务端交付要求。只有 API、数据库迁移或运行配置发生变化时才部署服务端；客户端改动不应触发无关的服务器操作。

## 验证与发布

- 数据语义变更至少运行 `npm run test:folded-categories`、`npm run test:feature`、`npm run test:storage`、`npm run test:analytics`、`npm run lint` 和 `npm run build`。涉及服务端时另在 `server/` 运行构建与测试。
- UI 变更必须在接近手机宽度的真实浏览器中点击验证：检查 `scrollWidth` 与 `clientWidth`、定位溢出元素、编辑关键字段、滚动到操作区，并确认没有向真实账本保存测试数据。临时设置和测试会话结束后恢复。
- 涉及 Android 能力时运行 `npm run android:build`，并核对升级安装的数据保留边界；不要用单独的资源同步成功替代 APK 构建与产物验收。
- 用户完成手机验收前，不把候选分支合入 `main`。获得明确许可后，先获取远端最新 `main`，优先快进合并；若出现分叉先审查差异。合并后复跑关键门禁，推送并直接核对远端 `main` 的提交。
