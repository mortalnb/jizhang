# Privacy Policy

智能记账是本地优先的开源记账工具。账本默认只保存在设备；模型调用和云同步均由用户主动触发。

## 本地保存

应用在 WebView LocalStorage 中保存账单、商品明细、分类、预算、模型配置、自填 API Key，以及云端登录会话和同步状态。Android 系统自动备份已关闭；请使用应用内 JSON 导出或可选云同步保存副本。

## 模型调用

- 文字解析会发送用户输入的文字。
- 图片识别会发送用户选择的账单截图。
- 应用内语音会采集最长 60 秒的音频，编码为 WAV，并发送给 `mimo-v2.5-asr`。
- 模型能力测试会发送测试提示和内置透明图片。

自填模型模式直接向用户配置的服务发送数据，项目作者不会接收 API Key 或请求内容。云端模式由作者服务器代转模型请求；API 日志会隐藏请求体和授权头，服务端记录账号、接口、模型、成功状态、耗时和语音时长，不持久化完整文字、图片或录音内容。

## 可选云同步

云同步默认关闭。开启后，作者服务器会为当前账号保存一份版本化账本快照，包括：

- 账单、商品明细、日期、金额、分类、标签和备注；
- 分类列表与月度预算。

云快照不包含自填 API Key、访问 token、刷新 token、密码或设备凭据。传输使用 HTTPS。服务端数据库能够读取快照明文，因此不应把它理解为端到端加密备份。

云端恢复不会自动执行，必须由用户确认；版本冲突时自动上传会暂停。退出账号或关闭同步不会删除已有快照。如需删除内测账号和云端账本，请联系项目维护者。

## 账号数据

内测云端服务会保存用户名、密码哈希、设备绑定、会话哈希、模型授权、用量记录和可选账本快照。不会保存明文密码或模型 API Key 到客户端。

## 第三方服务

账单、截图和语音可能包含消费记录、商户、商品、日期或其他敏感信息。使用前请确认你接受 MiMo 或自行选择服务商的隐私政策、计费和数据处理方式。

如果怀疑 API Key 或云端会话泄露，请立即吊销对应凭据并联系维护者。隐私问题可通过 GitHub Issues 或 `sqofficial0523@gmail.com` 反馈。
